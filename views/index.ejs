<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>MY APP</title>
</head>
<body>
May Node and Express be with you.

<ul class="quotes">
    <% for(var i=0; i<quotes.length; i++) {%>
    <li class="quote">
        <span><%= quotes[i].title %></span>
        <span><%= quotes[i].answer %></span>
    </li>
    <% } %>
</ul>
<a href="person" >aaaa</a>
<form action="/submitPicture" method="POST">
    <input type="text" placeholder="Title" name="title">
    <input type="text" placeholder="Answer" name="answer">
    <input type="text" placeholder="Answer" name="url">
    <button type="submit">Submit</button>
</form>
</body>
</html>